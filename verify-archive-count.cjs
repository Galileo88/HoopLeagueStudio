const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const h=fs.readFileSync('standalone/Hoopland League Studio.html','utf8');const a=h.indexOf('function renderArchive(){'),b=h.indexOf('\nfunction ',a+1);const nodes={};function el(tag,cls,text){return {tag,textContent:text||'',value:'',children:[],append(...xs){this.children.push(...xs);if(this.tag==='select'&&this.children.length===xs.length)this.value=xs[0].value||xs[0].textContent},replaceChildren(){this.children=[]},setAttribute(){}}}const assets=[{name:'A',path:'One/Logos/a.png',group:'One',kind:'Logos'},{name:'B',path:'One/Courts/b.png',group:'One',kind:'Courts'},{name:'C',path:'Two/Logos/c.png',group:'Two',kind:'Logos'}];const render=vm.runInNewContext(h.slice(a,b)+';renderArchive',{el,$:s=>nodes[s]||=(el('div')),assets,publicAssetURL:a=>!a.isLocal&&/^https?:/.test(a.url||'')?a.url:'',archiveLeague:a=>a.group,archiveSourceEditor(){},label:k=>k,refreshArchive(){}});render();const [filters,count]=nodes['#content'].children;assert.ok(count.textContent.startsWith('3 assets'));const [search,group,kind]=filters.children;group.value='One';group.onchange();assert.ok(count.textContent.startsWith('2 assets'));kind.value='Logos';kind.onchange();assert.ok(count.textContent.startsWith('1 asset shown'));search.value='missing';search.oninput();assert.ok(count.textContent.startsWith('0 assets'));console.log('Passed asset counts for all assets, league, type and empty search.');
