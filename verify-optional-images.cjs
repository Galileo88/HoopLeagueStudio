const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const s=fs.readFileSync('standalone/Hoopland League Studio.html','utf8');const a=s.indexOf('function isCustomImage('),b=s.indexOf('\nconst $=',a);const prepare=vm.runInNewContext(s.slice(a,b)+';prepareImages',{structuredClone,archiveURL:x=>x,validateImageSize:x=>{if(x.width>256)throw Error('Too large');return x}});(async()=>{const data={logoURL:'',logoSize:3,teams:[{name:'Talons',logoURL:'atlanta',logoSize:1,court:{overlayURL:''},frontOffice:{adsURL:'',adSize:3}}]};assert.deepEqual(await prepare(data,[],()=>{throw Error('Should not measure built-ins')}),data);const custom=structuredClone(data);custom.teams[0].logoURL='https://example.com/logo.png';const ready=await prepare(custom,[],async()=>({width:128,height:64}));assert.equal(ready.teams[0].logoSize,128);await assert.rejects(()=>prepare(custom,[],async()=>({width:512,height:512})),/Too large/);console.log('Passed built-in and empty image preservation, custom dimensions and size restrictions.');})();
